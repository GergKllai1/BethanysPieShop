@*Strongly typed view*@
@model PieListViewModel

<h1>@Model.CurrentCategory</h1>
@foreach (var pie in Model.Pies)
{
    <div>
        <h3>@pie.Name</h3>
        <h3>@pie.Price.ToString("c")</h3>
        <h3>@pie.Category.CategoryName</h3>
    </div>
}